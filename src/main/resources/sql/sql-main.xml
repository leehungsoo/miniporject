<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.aws402.miniproject.mapper.MainMapper">
  <select id="selectMemberLogin" parameterType="map" resultType="int">
    select count(*) cnt
    from tb_member
    where member_id=#{memberId} and member_pwd=#{memberPwd}
  </select>
  <select id="selectMemberDetail" parameterType="String" resultType="MemberDTO">
    select member_idx, member_id, member_name, member_phone, member_gender, member_email, member_join_date,
    member_level, member_status
    from tb_member
    where member_id=#{memberId}
  </select>
</mapper>