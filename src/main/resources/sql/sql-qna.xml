<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.aws402.miniproject.mapper.QnaMapper">
    <select id="selectQnaList" resultType="QnaDTO">
        SELECT
        board_idx,
        board_title,
        board_contents,
        board_create_idx,
        member_name,
        board_create_date,
        board_hit_cnt
        FROM tb_board join tb_member on (board_create_idx = member_idx)
        ORDER BY board_idx DESC
    </select>

    <!-- Q&A 글 등록 -->
    <insert id="insertQna" parameterType="QnaDTO">
        INSERT INTO qna (title, content, writer, created_at, cnt, answer_status, replied)
        VALUES (#{title}, #{content}, #{writer}, NOW(), 0, '답변대기', 'N')
    </insert>

    <!-- Q&A 상세 조회 -->
    <select id="selectQnaDetail" parameterType="int" resultType="QnaDTO">
        SELECT
        board_idx,
        board_title,
        board_contents,
        board_create_idx,
        member_name,
        board_create_date,
        board_hit_cnt
        FROM tb_board join tb_member on (board_create_idx = member_idx)
        WHERE board_idx = #{id}
    </select>

    <select id="selectQnaReply" parameterType="int" resultType="ReplyDTO">
        select
        reply_idx,
        reply_contents,
        reply_board_idx,
        reply_create_idx,
        reply_create_date
        from tb_reply join tb_member on (reply_create_idx=member_idx)
        where reply_board_idx = #{boardIdx}
        order by reply_idx desc limit 1
    </select>

    <!-- 조회수 증가 -->
    <update id="updateHitCount" parameterType="int">
        UPDATE tb_board SET board_hit_cnt = board_hit_cnt + 1 WHERE board_idx = #{qnaIdx}
    </update>

    <!-- Q&A 수정 -->
    <update id="updateQna" parameterType="QnaDTO">
        UPDATE qna
        SET title = #{title},
        content = #{content}
        WHERE id = #{id}
    </update>

    <!-- Q&A 삭제 -->
    <delete id="deleteQna" parameterType="int">
        DELETE FROM qna WHERE id = #{qnaIdx}
    </delete>

    <!-- 관리자 답변 등록 -->
    <update id="insertQnaAnswer" parameterType="QnaDTO">
        UPDATE qna
        SET answer = #{answer},
        answer_date = NOW(),
        answer_status = '답변완료',
        reply = #{answer},
        reply_created_at = NOW(),
        replied = 'Y'
        WHERE id = #{id}
    </update>
</mapper>